# Prefer repo-root `.env` — see /.env.example in the project root.
# This file is optional; merge these keys into root `.env` if you keep secrets here.

COMPOSE_PROFILES=caddy
DUCKDNS_API_TOKEN=
IRONDOME_PUBLIC_URL=https://irondome-playground.duckdns.org
